# Data Examples

Download the public artifacts with:

```bash
hf download ziyan98/plfb --repo-type dataset --local-dir ./plfb_artifacts
```

Then set:

```bash
export PLFB_ARTIFACT_ROOT=$PWD/plfb_artifacts
```
